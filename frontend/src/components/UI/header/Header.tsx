import React from 'react';
import './Header.scss';

const Header = () => {
    return (
        <div className="text-bar">
            <p>We create modern websites and promote them effectively</p>
        </div>
    );
};

export default Header;